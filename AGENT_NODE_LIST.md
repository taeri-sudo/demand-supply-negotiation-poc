# Step 2 Agent/노드 목록

agent/노드별 역할, 내부 단계, 판단 유형, 입력을 정리한다. State에서 참조되는
`role_tag`를 여기서 선언한다. 판단이 필요한 노드만 Agent로 분류.
변수/태그명은 영어(snake_case), 설명은 한글. 필드 정의는 STATE_SCHEMA.md,
agent 간 연결·신호·동시성은 GRAPH_FLOW.md 참고.

## 전체 구조 요약

```
forecast agent((회사,item) 인스턴스별 1개, 총 N개, role_tag: forecast) — 지속 태스크
   (시나리오 정의→데이터 수집·소스 판단→예측기법 선택→시나리오별 예측 계산→
    발생 가능성 평가→시나리오 선택을 한 agent 내부 단계로 수행)
        ↕  (신호 기반, 평소 정방향 최적화 / 예외 시에만 역방향 핸드오프)
supply_coordination agent(1개, role_tag: supply_coordination) — 지속 태스크
   ↕procurement_plan   ↕production_plan   ↕logistics_plan  (각 1개, 지속 태스크)
   (hub-and-spoke — 셋 다 직접 연결, 사슬 아님. 순서는 의존관계에 따른
    호출 순서일 뿐, 건너뛰기/역방향 되돌림 모두 구조적으로 가능)
        ↓
실제 조달/생산/배송 (그래프 노드 아님, 외부 경계 — sales_channel과 같은 성격)

validation agent(들) — 일감은 이벤트 트리거·무기억 워커풀 방식으로 받고,
판정(`passed`/`flagged`/`check_failed`)만 State에 쓴다 — 라우팅 권한은
없다. human_manager(들) — escalation 발생 시 반응(알림은 받기만 함),
지속 태스크 아님.

문제 발생 시: 공급망계획agent → supply_coordination → (필요시) forecast/
채널/사람 escalation — 어디까지 되돌릴지는 interaction_protocol이
규정.
```

**동시성 모델**: asyncio 단일 프로세스 — "지속 태스크"는 각자 독립된
asyncio 태스크로 서로 안 막히고 진행, "이벤트 워커풀"은 트리거될 때만 반응.
상세는 GRAPH_FLOW.md "동시성 모델" 참고.

## 지속 태스크 (판단·협상을 계속 담당)

물리적 실행이 아니라 **계획(약속·커밋) 판단**만 하므로 여기 속한다(아래
"외부 경계"와 구분).

### forecast agent (Agent, (회사,item) 인스턴스별 1개, 총 N개, role_tag: `forecast`)
시나리오 정의부터 시나리오 선택까지 한 agent가 내부 단계로 담당한다.
분석 agent를 따로 두지 않는 이유: 되돌림 재개 지점이 단계 경계와 일치해
(아래 "되돌림") 한 agent의 내부 단계로 충분하다. 기록 필드는 STATE_SCHEMA.md
`forecast_records` 참고.

**입력**
- 데이터(`kind` × `item_scope`, 정의는 STATE_SCHEMA.md). 현재 데이터로 채울
  수 있는 조합:

  | kind | item_scope | 현재 데이터 |
  |---|---|---|
  | `orders` | `same_item` / `similar_item` / `category` | 있음 (POS 기반 생성 수주, sales_channel 참고) |
  | `pos` | `same_item` / `similar_item` / `category` | 있음 (공개 POS 실데이터) |
  | `market` | `category` | 있음 (무료 정부 통계 대리값, 출처 미정) |
  | `market` | `same_item` / `similar_item` | 없음 (유료 조사 데이터) — 구조상 성립, 데이터 확보 시 바로 사용 |

- 주문 이력의 표시: 계약으로 들어와 납품된 **첫 주문**(초기 진열·재고 물량이
  포함돼 평소 수요보다 클 수 있음)과 **프로모션 기간**. 값은 그대로 두고
  표시만 남긴다.
- 프로모션: 과거 프로모션 이력(위 표시) + 예정된 **프로모션 일정**(언제, 어느
  고객사·item에 — 수량은 받지 않음).
- 고객사별 **계약 조건**: 최소 구매 약정(구속력 여부 포함).
- 신제품은 시장 데이터(`market`)를 중심으로 보고, 우리 유사 제품 데이터
  (`orders`/`pos` + `similar_item`)는 보조로 쓴다.

**내부 단계(순서대로)**
1. **시나리오 정의** (Agent, 판단) — 어떤 미래 가정들을 비교할지 정한다.
   시나리오에 따라 필요한 데이터가 달라지므로 맨 앞에 온다. 현재는 규칙이
   이 판단 자리를 채운다(`defined_by: "rule"`):
   - 기준 시나리오(현재 추세 유지)는 항상 1개
   - 근거 데이터가 있는 driver마다, 과거에 실제로 관측된 방향만 시나리오로
     추가(예: 시장 데이터에서 −5% 이상 위축과 +5% 이상 성장이 모두 있었으면
     2개 추가)
   - 확정된 입력(예정된 프로모션 일정, 가격 변경)은 별도 시나리오가 아니라
     **모든 시나리오의 전제**로 넣는다. 프로모션 `event` 가정은 고객사(`company_id`)와 예측 대상
       item(`item_id`)의 과거 프로모션 기간이 근거로 있을 때만 들어간다
   - 구속력 없는 최소 구매 약정이 있으면 "고객사가 약정대로 산다" 시나리오를
     추가한다
   - 거의 같은 시나리오는 시나리오 검증 조건(STATE_SCHEMA.md)으로 걸러진다
2. **데이터 수집 (함수) + 데이터 소스 판단 (Agent, 판단)** — 시나리오가
   요구하는 근거 데이터를 포함해 수집하고, 각 소스의 상태를 확인한다:
   - 기본 데이터(`orders` + `same_item`)가 있으면 **항상 쓴다** — 고객사
       (`company_id`)와 예측 대상 item(`item_id`)에 대한 유일한 직접 근거
   - **오염**(오류·이상치·결측·품절 구간) → 표준 정제 적용
   - **무관** → 관련 없는 데이터면 소스 제외, 구조 변화 이전이 현재와
     무관하면 그 날짜부터 사용(예: 계약 첫 주문이 평소 수요와 크게 다르면
      첫 주문 다음 기간부터)
   - **부족**(관측치 수가 모자라 추정이 불안정하거나 계절성 같은 패턴을
     볼 수 없음) → 그 상황에 **존재하는** 데이터로 보강(고정 우선순위 없음)
   - 보강할 데이터도 없으면 → ③ 사람 escalation
   - 자기 데이터가 쌓이면 보강은 자연히 빠진다. 섞는 비율은 표준 기법을 쓴다
3. **예측기법 선택** (Agent, 판단) — 데이터 특성(계절성/간헐수요 등)에 따라
   통계 예측기법을 고른다(LLM이 아니라 통계 기법). 표준 라이브러리 함수를
   그대로 호출하고, 자체 알고리즘 개발/하이퍼파라미터 튜닝은 안 함
4. **시나리오별 예측 계산** (함수) — 같은 데이터·같은 예측기법으로 기준
   예측을 만들고, 시나리오마다 가정의 `demand_effect`를 적용해 예측값과
   불확실성을 산출. 한 시나리오 안의 여러 가정 효과를 합치는 방식은 표준
   방식을 쓴다. 프로모션 효과는 과거 프로모션 기간의 실제 주문 ÷ 평소 수준
5. **발생 가능성 평가** (판단) — 과거 이력에서 각 시나리오의 가정에 해당하는
   기간이 얼마나 자주 있었는지(빈도)를 세고, 시나리오들끼리 합이 1이 되도록
   나눈다(예: 36개월 중 추세 유지 20개월·위축 10개월·성장 6개월 →
   0.56/0.28/0.17). 시나리오 정의와 같은 기준(예: ±5%)으로 기간을 나눈다.
   "약정대로 산다" 시나리오는 고객사(`company_id`)의 과거 약정 이행
       이력으로 매긴다.
   근거가 부족해 빈도를 낼 수 없으면 "애매함"으로 표시
6. **시나리오 선택** (Agent, 판단) — 판단3계층:
   - ① 규칙: `cost_estimate`가 가장 낮은 시나리오 선택
   - ② 판단: 상위 두 시나리오의 `cost_estimate`가 거의 같으면 "애매함" 표시
     (현재는 표시 후 규칙대로 선택)
   - ③ 사람: 시나리오끼리 예측값이 크게 갈리는데 발생 가능성도 비슷하면
     escalation
   - **최소 구매 약정**: 구속력 있는 약정(take-or-pay)이면 약정 잔여량을
     선택값의 하한으로 적용. 구속력이 없으면 위 "약정대로 산다" 시나리오로
     `cost_estimate`가 비교한다. 두 경우 모두 예측과 약정의 차이가
     `interaction_protocol`의 알림 기준(구속력 없음이 더 낮음)을 넘으면
     human_manager에게 **알림**만 보내고 진행을 멈추지 않는다
   - 선택된 시나리오의 예측값은 평소엔 supply_coordination에게 단방향으로
     전달된다(GRAPH_FLOW.md "상호작용 세 가지 유형"의 최적화)

**되돌림(재실행)** — 재실행되는 판단 함수는 `suspected_cause`와 덮어쓰기 전
직전 결과를 입력으로 받는다. 같은 스냅샷에서 입력이 같으면 결과도 같아야
하므로, 사유를 입력에 넣어야 재실행이 다른 결과를 낸다. 대응은 우선순위가
아니라 **사유**를 기준으로 한다:

| suspected_cause | 재개 지점 | 대응 |
|---|---|---|
| `scenario` | 1단계(시나리오 정의) | 문제가 된 가정을 빼거나 고친 뒤 다시 정의 |
| `data_source` + `insufficient` | 2단계(데이터 수집) | 보강 소스 추가 |
| `data_source` + `contaminated` | 2단계 | 해당 소스에 정제 재적용·강화 |
| `data_source` + `irrelevant` + `use_from` 있음 | 2단계 | 해당 소스를 그 날짜부터 사용 |
| `data_source` + `irrelevant` + `use_from` 없음 | 2단계 | 해당 소스를 `excluded_sources`로 이동 |
| `forecast_method` | 3단계(예측기법 선택, 수집 데이터 재사용) | 직전 예측기법을 제외하고 재선택 |

재개 지점 이후 단계는 전부 다시 돈다. "시나리오 선택만 다시"라는 별도 재개
지점은 두지 않는다 — 앞 단계부터 다시 돌리면 선택도 자연히 다시 이뤄진다.
대응할 선택지가 소진되면(예: 보강할 데이터가 더 없음) ③ 사람 escalation.
되돌림을 보내는 쪽(검증agent, supply_coordination)은 GRAPH_FLOW.md 참고.

### supply_coordination agent (Agent, 1개, role_tag: `supply_coordination`)
- **후보안(allocation_candidates) 생성·선정** (Agent, 판단)
- **우선순위 점수 산출** (함수 — 가중치 조정 등은 판단 여지 있음). "몇 번까지
  기다릴지"(`max_rounds`, 안전장치)와 "누구부터/얼마나 배분할지"(우선순위)는
  다른 문제다. 우선순위는 **동점 처리(tie-breaker) 순서**로 산출한다 — 4단이
  매번 적용되는 게 아니라, 앞 기준에서 확실한 차이가 안 날 때만 다음 기준으로
  내려감:
  1. **매출/마진 임팩트** (주 기준) — 대부분 이 값만으로 우열이 갈림
  2. **예측 신뢰도 보정** — 1번이 임계치 이내로 비슷할 때만 적용, 과거 예측
     정확도 이력으로 순위 조정(자주 틀린 채널은 하향)
  3. **Aging 보정(하한선)** — 1·2번도 비슷할 때만 적용, 대기시간에 비례해
     보정. "정하는 기준"이 아니라 "굶주림 방지 하한선"
  4. **배분량(capacity)** — 축이 아니라 위 순서로 정렬한 뒤 결정되는 산출물
- **예측 신뢰도(`forecast_reliability`) 입력**: walk-forward validation(과거
  데이터를 시간순으로 잘라, 그 시점까지의 데이터로 다음 구간을 예측 → 실제값과
  비교 → 기준 시점을 한 칸씩 밀며 반복하는 표준 검증 방식)으로 계산한다.
  시뮬레이션에서는 데이터의 시간단위(일/주/월)는 그대로 두고 재생 속도만
  빠르게 한다. 다음 계획주기에도 남아야 하므로 경량 저장소(SQLite 등)에
  영속화한다 — LLM 파인튜닝이 아니라, LLM 밖의 참고 숫자를 남겼다가 다음
  판단 시 제공하는 것
- **계약 조건 적용**: 고객사별 **공급 보장 물량**은 배분 시 우선 확보하고,
  capacity 부족으로 채우지 못하면 escalation(M5). **최소 주문 수량(MOQ)**은
  배분량을 그 단위에 맞추는 규칙(M4)
- **공급망계획agent 문제 신호 처리** (Agent, 판단) — 자체 재조정으로 풀지,
  위/옆(forecast·채널·사람)으로 되돌릴지 결정. 같은 사유
  (`routing_reason` 등)가 `interaction_protocol.repeat_escalation_threshold`
  회 연속 반복되면 "이 선에서 구조적으로 안 풀림"으로 간주해 `max_rounds`
  소진을 기다리지 않고 상위로 확장
- 공급망계획agent 셋과의 연결 구조(hub-and-spoke)는 GRAPH_FLOW.md 참고

### procurement_plan agent (Agent, 1개, role_tag: `procurement_plan`)
어느 공급처에서, 언제까지, 얼마에 조달할지 **결정(커밋)**까지가 판단.
판단 전에 **과거 리드타임/신뢰도 패턴 조회(함수)+추정(판단)** 단계가
선행됨 — 공급처 선택과 발주 절차가 명확히 다른 카테고리의 판단이 아니라서
별도 agent로는 안 쪼갬. 실제 자재가 도착하는 물리적 과정은 외부 경계.
공급업체 선정·계약관리는 이미 전제로 주어진 것으로 스코프 밖(발주/주문
처리만 담당). **여러 (회사,item) 인스턴스의 요청을 1개 agent 내부에서
안건별로 처리** — 공급처 capacity처럼 공유자원을 다루는 agent는 나눌 수
없음(supply_coordination을 1개로 둔 것과 같은 논리). `capacity_pools`
접근 권한은 STATE_SCHEMA.md 참고.
**데이터 소스**: 실거래 데이터 없음 → 업계 KPI(평균)+표준편차 기반 확률분포
샘플링(추정 입력 + 사후 결과 확인, 양쪽에 재사용, 고정값 아님).

### production_plan agent (Agent, 1개, role_tag: `production_plan`)
생산 일정·라인 배정 **스케줄링 결정**까지가 판단, 과거 리드타임/불량률
패턴 추정 단계 선행(위와 동일한 이유로 별도 agent 분리 안 함). 실제 라인이
도는 것(원자재 투입, 라인 실행)은 외부 경계 — 실제 생산/조립 실행·
품질테스트·인바운드/창고운영은 **제외**(물리적 실행 영역). 공유/전용 라인
전환(초기엔 여러 품목이 changeover로 한 라인을 공유하다, 수요가 확인되면
전용라인 전환)도 이 스케줄링 판단 범위. 여러 (회사,item) 인스턴스 요청을
1개 agent 내부에서 처리(생산라인이라는 공유자원). `capacity_pools`를 직접
접근하는 유일한 plan agent(STATE_SCHEMA.md 참고).
**데이터 소스**: 업계 KPI(평균)+표준편차 기반 확률분포 샘플링(추정+결과
확인 겸용, 고정값 아님).

### logistics_plan agent (Agent, 1개, role_tag: `logistics_plan`)
배송 자원·일정 **배정 결정**까지가 판단, 과거 배송 지연 패턴 추정 단계
선행. 실제 운송/배송 실행은 외부 경계. 여러 (회사,item) 인스턴스 요청을
1개 agent 내부에서 처리(배송capacity라는 공유자원). `capacity_pools` 접근
권한은 STATE_SCHEMA.md 참고.
**데이터 소스: SynDelay**(실데이터로 학습된 생성모델의 합성 데이터, 추정
입력 + 사후 결과 확인 겸용).

## 검증agent (워커풀 방식으로 일감을 받되, 판정만 하고 라우팅은 안 함)

### validation agent(들) (Agent, 도메인별로 분리 — role_tag: `forecast_validation`/
`procurement_validation` 등)
- **일감을 받는 방식은 워커풀**(트리거될 때만 반응, 무기억 — 매번 State를
  새로 읽고 판단, 자기만의 작업 히스토리를 안 가짐. 워커가 몇 명이든
  아무나 집어가도 문제없음) — 지속 태스크가 아님
- **판정만 하고 라우팅 권한은 갖지 않는다** — "문제가 생겼을 때 그걸
  어디로 되돌릴지"는 이미 각 agent 자신의 역할(forecast의 되돌림,
  supply_coordination의 "공급망계획agent 문제 신호 처리" 등)에 있는 권한이라
  중복되면 안 됨
- **판단 규칙은 대상 agent의 계산을 재현하지 않고, 독립적 제약조건만
  확인** — 계산을 재현하면 같은 입력에 항상 같은(통과) 결과만 나오는 죽은
  검증이 됨. 예: 배분 합계가 `capacity_pools` 총량을 넘는가, `role_tag`가
  실제로 그 필드 쓰기 권한이 있는가, 시나리오 검증 조건(STATE_SCHEMA.md
  `forecast_records`) — ①규칙(빠른 함수)으로 처리되고, "이 근거가 지금
  상황에서 타당한가" 같은 애매한 판단만 ②agent판단(LLM)으로 감
- 판정 결과(`validation.status`)는 검증 대상 record에 직접 쓴다. 판정별로
  누구에게 어떻게 전달되는지, 그리고 검증agent 자신의 오판 한계는
  GRAPH_FLOW.md "검증agent" 참고

## 외부 경계 (그래프 노드 아님, 판단 없음)

물리적으로 실제 일이 일어나거나 외부에서 데이터가 들어오는 곳 — 우리가
판단하지 않고, 결과만 데이터로 받음.
- 모든 외부 데이터는 **하나의 인터페이스 함수로 추상화**해서 읽는다 — 나중에
  실제 회사 데이터/API로 교체할 때 이 함수 내부만 바꾸면 되고, agent 판단
  로직은 안 건드림
- 내부 데이터 형식은 **공급망 표준 기준**: 식별자는 GS1(상품=GTIN,
  거래처·장소=GLN), 필드는 EDI 문서 기준(850=발주서→우리 수주,
  852=Product Activity Data→유통사가 공유하는 판매·재고). 어떤 데이터셋을
  쓰든 인터페이스 함수가 이 형식으로 변환한다
- **데이터 대체 가정**(DESIGN.md에 명시): 공개 POS 데이터셋의 매장(store)
  식별자는 인터페이스 함수 안에서만 고객사(`company_id`)로 간주해 매핑하고,
  원본 파일은 수정하지 않는다. 인스턴스는 고객사 단위이므로 고객사별·월별로
  합산해 쓴다. 공개 POS에 있는 상품은 모두 우리 회사 제품으로 간주한다

### sales_channel (role_tag: `sales_channel`)
디지털영업(3.5.8) 등 — 입력과 출력 둘 다.
- **입력**: forecast agent에 주문 이력(표시 포함), 고객사가 공유한 POS,
  프로모션 일정, 고객사별 계약 조건(최소 구매 약정·공급 보장 물량·MOQ)을
  제공. forecast를 거치지 않는 수량(새 고객사 첫 주문, 신제품 첫 물량,
  프로모션 이력이 없는 고객사·item의 프로모션 수량)은 human_input으로
  supply_coordination에 바로 들어가고, 납품된 뒤에는 주문 이력의 한 기간이
  된다
- **수주 데이터 생성기**: 공개 B2B 수주 데이터가 없으므로, 공개 POS 위에
  고객사의 재고 정책(표준 재고정책, 예: 재고가 기준 아래로 떨어지면 채우는
  방식, 정기 발주)을 시뮬레이션해 주문 이력을 생성한다. 생성기는 고객사의
  행동을 대신하는 외부 경계이며, 조건:
  1. forecast agent는 결과(POS, 주문)만 받는다 — 현실의 공급사도 고객사의
     재고 정책을 모른다
  2. POS와 주문의 관계를 **완벽하게 만들지 않는다**(정책 파라미터의 시기별
     변화, 발주 타이밍의 불규칙성, 수동 조정 같은 예외 포함) — 그렇지
     않으면 forecast가 POS로 주문을 역산해 예측 정확도가 부풀려진다
  3. 고객사마다 정책 파라미터를 다르게 둬 고객사별 특성을 만든다. MOQ가
     있으면 발주가 그 단위를 따른다
  4. 한 번 생성해 **고정 파일**로 저장하고, 실행 때마다 다시 만들지 않는다
- **출력**: supply_coordination의 배분 결정을 받아 그 안에서 거래

### 물리적 조달·생산·배송 실행
각 계획agent의 결정이 실제로 이행됐는지(리드타임, 불량률, 배송지연)의
결과값만 `exchanges[i].response_status`/`response`로 돌아옴.

## 사람

### human_manager (사람, Agent 아님, role_tag: `human_manager`)
지속 태스크가 아니라 이벤트 워커풀과 같은 성격 — escalation_records에
새 항목이 생기면 그때 반응(대시보드). escalation_records를 통해 개입 시점과
사유가 기록됨. **알림**(예: 최소 구매 약정과 예측의 차이)은 받기만 하고
진행을 멈추지 않는다. 알림도 `escalation_records`에 기록되며,
`mode: "notice"`로 멈추는 escalation(`mode: "intervention"`)과 구분된다.

## 아직 확정 안 된 것

- supply_coordination agent의 "우선순위 점수 산출"이 순수 함수인지 일부
  판단이 섞이는지 — 실제 구현하면서 재확인
