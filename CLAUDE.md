# CLAUDE.md

S&OP(Sales & Operations Planning) 개념 기반, 판매예측↔공급망조율 멀티에이전트 POC.
여러 agent가 실제로 서로의 판단에 영향을 주는 구조(다회 협상)를 구현하는 것이
이 프로젝트의 핵심 목표다.

## 문서 언어 안내

README.md는 영문판(README.en.md)을 별도로 둘 수 있지만, CLAUDE.md/DESIGN.md/JOURNAL.md는
한국어 버전만 유지한다. 코드 작업 시 참고하는 원본은 항상 이 한국어 파일들이다.

## 작업 전 필수

**코드를 수정하기 전에 DESIGN.md를 반드시 읽을 것.** State 필드나 agent를 추가·변경했다면
DESIGN.md도 같이 갱신한다.

JOURNAL.md는 매 세션 통째로 읽지 않는다. 다음 상황에서만 확인한다:

1. 이미 시도했다가 폐기된 접근인지 확인이 필요할 때
2. DESIGN.md의 결론만으로는 "왜 이렇게 됐는지" 설명이 부족할 때
3. 사용자가 명시적으로 과거 논의 여부를 물을 때

## 참고 문서

- **STATE_DRAFT.md** — State 8개 최상위 필드 스키마. State 관련 코드 작업 전 확인.
- **AGENT_NODE_LIST.md** — agent/역할(role_tag) 목록, 각자의 판단 범위, 검증agent 작성
  원칙. agent 관련 코드 작업 전 확인. **새 agent/역할을 추가하기 전, 여기 기준으로
  기존 agent에 흡수될 수 있는지 먼저 검토한다.**
- **GRAPH_FLOW.md** — agent 간 신호 흐름, 검증 게이트, 엣지별 반복/종료조건.

세 문서 모두 자체완결적으로 쓰여 있어 이 파일들만 읽어도 이해된다.

## JOURNAL.md 기록 기준

git commit으로 복원되지 않는 것만 남긴다:

- 코드에 반영되지 않고 폐기된 시도/가설
- 여러 대안 중 하나를 택한 논거와 기각 이유
- 재현 절차, 그리고 재현으로 새롭게 드러난 사실
- 예상과 다르게 나온 결과(비결정론적 판단 등 특이 관측)

다음은 기록하지 않는다(커밋 메시지/테스트 로그로 복원 가능):

- "pytest 전체 통과"처럼 새로운 정보 없이 통과만 확인하는 문장
- 이미 DESIGN.md/커밋 메시지에 있는 결론의 단순 재진술

## 값 중복 방지

바뀔 수 있는 값(버전, 개수, 필드 목록 등)은 대표 문서 한 곳에만 두고, 다른 곳에서는
그 문서를 참조하게 서술한다. 값을 수정할 때는 다른 문서의 같은 항목도 grep으로 찾아
함께 갱신했는지 확인한다.

## 용어·문체 원칙

비유·관용구로 상태나 동작을 묘사하지 않는다(신체적 동작·물리적 손상·유전적 계승 등에
빗댄 표현). 실제로 무엇이 일어나는지 기술적으로 정확한 표현으로 쓴다. 다만 기술 문서에서
이미 표준 용어로 굳어진 표현(모의하다/mock, 확인하다, 수정하다 등)까지 바꾸지 않는다.

## 코드 관례

- 함수명·역할 태그는 snake_case 영어, docstring·print·로그 메시지는 한국어.
- 로깅은 `logging` 없이 `print()`. 형식은 `[role_tag:함수명] key=value`.
- ID는 `f"{PREFIX}-{uuid.uuid4().hex[:8].upper()}"` 꼴, 타임스탬프는
  `datetime.now(timezone.utc).isoformat()`.
- State는 pydantic `BaseModel`로 정의한다. 부분 갱신은 `model_copy(update={...})`를
  쓰고, `cast()` 없이 가는 것을 목표로 한다(막히면 JOURNAL.md에 이유를 남긴다).
- 구조화된 LLM 출력(판단+근거)도 pydantic 모델로 정의한다.
- 모든 State 읽기/쓰기는 `role_permissions`를 검사하는 wrapper 함수(`get_field`/
  `set_field`)를 통해서만 한다. 직접 State를 건드리지 않는다.

## 실행

TBD — 구현 진행하며 채운다.
